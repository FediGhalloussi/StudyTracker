query GetTasks {
  getAllTasks {
    id
    title
    type
    scheduledAt
    duration
    done
  }
}

query GetTasksByDate($date: String!) {
  getTasksByDate(date: $date) {
    id
    title
    scheduledAt
    duration
    done
  }
}

mutation DeleteTasks($id: String!) {
  delete(id: $id)
}
