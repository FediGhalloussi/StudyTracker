query GetAllAssignments {
  getAllAssignments {
    id
    title
    dueAt
    status
    subject {
      id
      name
    }
  }
}

mutation DeleteAssignment($id: String!) {
  deleteAssignment(id: $id)
}
